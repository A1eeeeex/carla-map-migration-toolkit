# Publication redaction report

Updated: 2026-09-08

## Authorized experimental publication update

The maintainer authorized experimental v0.1.0 publication. The release branch
is based on the previously audited remote history and contains only the explicit
191-file candidate, not unpublished local development commits or internal refs.

Two maintainer-enumerated Actions runs were inspected on September 8: two
executed host-job logs, two skipped engine jobs and empty artifact lists. The
logs had no approved-private-term or common credential-pattern findings;
credential values were masked. GitHub-hosted runner/tool-cache paths were not
mistaken for private workstation paths. Enumeration is attributed to the
maintainer's paginated API output; connector job/artifact reads are first-page
wrappers. No full raw-log export is claimed or included in the source release.
Final commit identity and new CI remain execution-time checks, not implied PASS.

## Current source-candidate closeout

The 191-file allowlisted source candidate contains only UTF-8 text. Its names
and contents were checked against the owner-approved 60-entry private literal
set with no findings. Prohibited asset/binary extensions, local-path patterns
and secret-assignment checks also passed. The exact source/hash manifest and
source-only archive remain local; neither includes Git history, raw evidence,
map assets, screenshots or engine binaries.

The source snapshot's text scan did not cover archive ownership/time headers.
A subsequent packaging check found that metadata in the old local backup.
The distributable source archive now uses neutral owner IDs, empty owner names,
fixed timestamps and no PAX headers. Its exact text membership and hashes were
checked separately; the original local backup was not overwritten.

The unchanged production core reuses the September 5 result of 435 host tests.
A fresh Python 3.10.12 source snapshot passed 22 targeted installation,
structure, publication and demo checks. Codex CLI 0.153.4 installed the Plugin
as installed/enabled in isolated state; the anonymous demo reported L1 PASS
and route validation NOT_RUN. A separate current-description routing batch
passed 11/11 cases. This is not full installed-invocation accuracy evidence.

These results replace the older snapshot counts below for the local source
candidate only. They do not close the retained GitHub Actions export gate,
establish an exact committed clone or manufacture complete current-contract
Verified Runs. See the [release checklist](docs/release-readiness.md) for the
proposed experimental release grade and the unchanged strict RC requirements.

## Historical audit snapshot — 2026-09-01

The following records and remaining actions describe the earlier snapshot.
The new local candidate checks above do not repeat or extend its remote audit.

## Scope

The candidate publication allowlist is limited to `plugins/`, `tests/`, `evals/`,
`docs/`, `development/shared/`, `.agents/plugins/`, `.github/workflows/`, the
root README and policy files, and the root implementation/verification reports.
Original handoff prompts, discovery records, drafts, private inputs, mounted
external environments, full logs, and historical evidence are consolidated
under the Git-ignored `development/local/` tree and are not publication
candidates.

`PUBLICATION_ALLOWLIST.json` enumerates every candidate file. Its integration
test rejects unlisted files under the candidate roots, forbidden binary/asset
suffixes, common host paths, secret assignments, and configured private
identifiers. Synthetic redaction-test literals are the only content-scan
exceptions and are named explicitly in that manifest.

## Results

- Pattern-based checks found no customer or legacy map identifier in the
  candidate allowlist. The owner-approved 60-entry private literal set now
  supplies the authoritative external scan input and remains outside Git.
- No real local host/mount path was found in the candidate allowlist.
- No candidate file has an Unreal/map/export extension such as `.uasset`,
  `.umap`, `.uexp`, `.ubulk`, `.fbx`, `.udatasmith`, or `.xodr`.
- No CARLA/Unreal/RoadRunner binary or private asset was copied.
- No credentials were found. The only credential-like strings are synthetic
  values inside the redaction unit test.
- The only email/network-like values are the reserved `.invalid` email and the
  documentation-only `192.0.2.0/24` address used to test redaction.
- Policy documents contain generic words such as “customer” to state prohibited
  content; these are not identifiers.
- Legacy source code remains `copied_into_candidate: false` in the source
  inventory because publication rights are unknown.
- The private source register remains redacted, while a separate owner-approved
  private literal set was reconstructed from the hash-verified source set and
  retained outside the candidate.
- The release publication audit now fails closed on a missing or empty literal
  set, a missing, empty or symlink-bearing external export, or a dirty
  worktree. It scans the current tracked tree, all reachable Git objects and
  refs available in the local candidate, and an unpacked external export
  without echoing matching literals. It does not determine whether that export
  is complete or whether the private GitHub repository has additional refs.
- The private GitHub ref mirror and all 187 reachable objects passed the
  authoritative literal and prohibited-binary-path audit. The authenticated
  private Actions log/artifact export audit is still `NOT_RUN`.
- The first current real UE4.27 target-stage candidate was recorded, scanned
  and then withdrawn when final log review found a material acceptance failure.
  After a bounded repair, a fresh six-check receipt and five-view review passed;
  the host recorder produced a new sensitive-term-scanned stage candidate under
  the ignored private evidence root. It has not been promoted to
  `evidence/verified/` because complete-route L5 evidence is still missing. The
  map, absolute paths, screenshots, raw logs and binaries remain excluded.
- A public L1 quickstart now generates original anonymous text placeholders only
  inside an explicit empty temporary directory. No generated Datasmith-shaped
  file or OpenDRIVE file is stored in the candidate. This does not close the
  rights-safe real Editor replay requirement.
- A verified individual copyright holder and accountable maintainer is recorded
  in the legal attribution and citation metadata. Original public-candidate
  material is licensed under Apache-2.0; excluded private and third-party
  material is not relicensed.
- The exact-file allowlist and all repository tests passed in the current
  implementation worktree: `379 passed` on 2026-09-01 with third-party pytest
  plugin autoload disabled to isolate the repository test environment.
- An allowlisted disposable snapshot passed all 379 tests under the
  supported Python 3.10.12 environment and installed Plugin 0.1.0 as
  installed/enabled in an isolated Codex 0.148.0 configuration. The host Codex
  configuration remained unchanged. Three independent ephemeral read-only
  sessions selected the correct Plugin-qualified Skill for one canonical
  prompt on each route. Because this is not an exact committed release
  candidate, it remains rehearsal rather than final clean-clone evidence.
- The current 182-file allowlisted snapshot separately passed a scan against all
  60 approved private literals. Only the private-set fingerprint and count were
  recorded; no private term was copied into the public candidate. This scoped
  snapshot scan does not replace the required exact-commit history and external
  Actions-export audit.
- The fail-closed publication audit now independently rejects forbidden
  map/archive/log suffixes and non-UTF-8 or NUL-bearing blobs in the current and
  historical public Git tree. Binary external artifacts are blocked until
  safely unpacked or converted to reviewed UTF-8 text.

## Remaining required pre-publication actions

1. Export all private GitHub Actions logs and artifacts with authenticated
   completeness evidence, safely unpack them, and run the publication audit.
2. Extend the rights-safe L1 quickstart with a redistributable real Editor replay
   fixture; do not repurpose the private validation map or screenshots.
3. Complete current-contract replay coverage for the remaining route stages and
   routes, or explicitly reduce the release claim instead of relabelling old
   evidence.
4. Run model trigger evals and publish only audited aggregate results.
5. Repeat the successful candidate-snapshot installation and three-session
   Skill discovery against the exact release-candidate commit.
6. After final publication authorization, enable and independently verify
   GitHub private vulnerability reporting before announcing the repository.

A private GitHub repository was created and `main` was pushed only after explicit
user authorization. The repository remains `PRIVATE`; `development/local/` was
excluded by Git, and no public release, asset upload, or visibility change was
performed.
