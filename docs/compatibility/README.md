# Compatibility evidence

The schema fixture is intentionally `experimental`. There is currently no public
evidence bundle that can justify `verified` or `community-verified` for any stack.

Add a compatibility entry only with exact route, versions, engine, platform, map
mode, toolkit version, evidence bundle, date, verifier, and limitations. Real
assets stay outside the public repository; publish only authorized redacted
summaries and manifests.
